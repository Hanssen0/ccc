---
"@ckb-ccc/core": major
---

Remove the deprecated `mol` aliases for `Entity`, `codec`, `Codec`, `uint`, `uintNumber`, `CodecLike`, `DecodedType`, and `EncodableType`. Import their top-level `ccc` equivalents instead.
